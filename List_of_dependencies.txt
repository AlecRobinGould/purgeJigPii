# sudo apt install git
# sudo apt install python3-pip
# pip3 install -r List_of_dependencies.txt
# sudo apt-get install screen
# change in /boot/config.txt dtparam=i2c_arm=on,i2c_arm_baudrate=400000 #100kHz seems to work.

numpy>=1.8.0
smbus2>=0.4.2
smbus
RPi.GPIO>=0.7.1
multiprocess
psutil